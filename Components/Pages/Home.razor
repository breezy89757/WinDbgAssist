@page "/"

<PageTitle>WinDbg Assist</PageTitle>

<div class="landing-page">
    <div class="content-box">
        <h1 class="glitch-text">WINDBG ASSIST</h1>
        <p class="subtitle">AI-POWERED AUTOMATED DEBUGGING TERMINAL</p>
        
        <div class="actions">
            <a href="/analysis" class="btn-primary">進入自主診斷中心</a>
        </div>
    </div>
</div>

<style>
    .landing-page {
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: radial-gradient(circle at center, #1b1b3a 0%, #0f0f1a 100%);
    }

    .content-box {
        text-align: center;
    }

    .glitch-text {
        font-size: 4rem;
        font-weight: 900;
        letter-spacing: 0.5rem;
        color: #fff;
        margin-bottom: 0.5rem;
        text-shadow: 0 0 10px rgba(102, 126, 234, 0.5);
    }

    .subtitle {
        color: #667eea;
        font-size: 1rem;
        letter-spacing: 0.3rem;
        margin-bottom: 3rem;
        font-weight: 300;
    }

    .btn-primary {
        display: inline-block;
        padding: 1rem 3rem;
        background: transparent;
        border: 1px solid #667eea;
        color: #667eea;
        text-decoration: none;
        border-radius: 4px;
        font-weight: bold;
        transition: all 0.3s;
        text-transform: uppercase;
        letter-spacing: 0.1rem;
    }

    .btn-primary:hover {
        background: #667eea;
        color: #fff;
        box-shadow: 0 0 20px rgba(102, 126, 234, 0.4);
        transform: translateY(-2px);
    }
</style>

